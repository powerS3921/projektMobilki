<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    android:label="user_set_layout"
    app:startDestination="@id/userSet3">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.hangman.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/gameLayout"
        android:name="com.example.hangman.GameLayout"
        android:label="game_layout"
        tools:layout="@layout/game_layout" />
    <navigation android:id="@+id/navigation"
        app:startDestination="@id/userSet3">
        <activity
            android:id="@+id/userSet3"
            android:name="com.example.hangman.userSet"
            android:label="userSet" />
        <action
            android:id="@+id/action_global_mainActivity"
            app:destination="@id/mainActivity" />
    </navigation>
</navigation>